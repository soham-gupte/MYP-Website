import "./AboutUsStyles.css";

export function AboutUs() {
    return (
        <div className="about-us">
            {/* <h1>About Us.</h1> */}
            {/* The goal is to keep another navbar in the about us page to ensure the page doesn't
            require excessive scrolling, thus ensuring the user to navigate through the webpage 
            with more ease */}
            {/* Add 2x2 grid for Content and GIF blocks */}
        </div>
    )
}